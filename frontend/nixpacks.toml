# Nixpacks configuration for Railway
# This tells Railway to use Node.js, build the React app, and serve it

[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
